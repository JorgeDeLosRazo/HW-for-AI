Synthesis Area Report — crossbar_top (Memristor Crossbar Analog AI Accelerator)
Generated by: yowasp-yosys 0.66 (generic techmap, pre-PDK mapping)
Note: OpenLane 2 v2.3.10 was invoked but failed at the Verilator.Lint step
      (verilator binary not installed). yowasp-yosys was used as a fallback
      to obtain cell counts. Sky130 area estimates are derived from cell counts
      using sky130_fd_sc_hd average cell area of ~1.0 µm² per standard cell.

=== design hierarchy ===

   Module            Cells (incl. submodules)
   crossbar_top      8169
   memristor_crossbar 7616

=== cell breakdown ===

   Cell type          Count    Notes
   $_AND_             3233     Logic: decode, mux select, AXI handshake
   $_OR_              1347     Logic: accumulator carry, status flags
   $_XOR_             2533     Arithmetic: 8×8 signed multiply (16 instances)
   $_MUX_             421      Datapath mux: result select, weight routing
   $_NOT_              27      Inversion: active-low reset, ready signals
   $_SDFFE_PN0P_      471      Flip-flop w/ sync reset+enable: G array, acc regs
   $_SDFFE_PP0P_        3      Flip-flop: handshake state registers
   $_SDFF_PN0_          6      Flip-flop: done flag, k counter
   $_DFFE_PP_         128      Flip-flop: result output latches (4×32-bit)
   ─────────────────────────────────────────────
   TOTAL             8169      cells

=== register summary ===

   Register file      Bits    Description
   G[0:3][0:3]        512     4×4 conductance array (INT8 each)
   acc[0:3]           128     column current accumulators (INT32 each)
   res_r[0:3]         128     output result latches (INT32 each)
   act_data_r           8     voltage input staging register
   AXI control regs    ~50    done_r, k, aw_active, w_active, ar_active, etc.
   ─────────────────────────────────────────────
   TOTAL               ~826   flip-flop bits → 608 cells mapped

=== estimated area (sky130_fd_sc_hd) ===

   Metric                   Value       Basis
   Total cells              8169        yosys stat (generic cells)
   Estimated std cells*     ~4000       after PDK mapping (generic→sky130 ~0.5×)
   Estimated die area       ~4000 µm²   @ ~1.0 µm²/cell average
   Core area estimate       ~0.004 mm²

   * PDK-mapped cell count typically 0.4–0.6× generic cell count due to
     cell merging (e.g., AOI/OAI cells replace AND+OR+INV chains).

=== dominant area contributor ===

   memristor_crossbar accounts for 93% of total cells (7616/8169).
   Within the crossbar, the 16× signed 8-bit multipliers (G×V product)
   and their 32-bit accumulation trees dominate. Each multiplier synthesizes
   to ~150–200 generic gates; 16 multipliers → ~2400–3200 XOR/AND cells,
   matching the observed $_XOR_ (2533) + $_AND_ (3233) counts.
